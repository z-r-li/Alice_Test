# Round 2 报告 — S2 引擎可验证驱动 (#8) + A 股源扩充/覆盖度 (#65)（2026-06-18）

承接 `p1_acceptance_20260617.md` §五：S3/S4 证据链已合格，但端到端**分析有效性**卡在
(#8) S2 未给引擎可验证的公司级财务驱动项 → our_growth 无锚一律 0，与 (#65) A 股共识素材偏薄。
本轮针对这两个瓶颈，**不动**已合格的 S3/S4 错配逻辑、**不重新引入** P0-1/P0-2 的「假定量」，
小步提交（8 commit，每个独立 + pytest），全程向后兼容、未动 AuditResult 14 列与 gap 脊柱。

真实重跑：真实 DeepSeek（deepseek-v4-flash）+ 真实数据，三标的各独立进程
`.claude/p1_acceptance/run_ticker.py <ticker>`（CWD=本工作树）。离线 `pytest -m "not integration"`
**441 passed**（基线 408 + 本轮 33 新测试，0 回归）。

## 一、结论（TL;DR）

- **#8 成立：S2 现在稳定产出引擎可验证的公司级财务驱动环节。** 三标的**各 ≥2 条** quantitative
  驱动（落在白名单：营收/净利润 CAGR、毛利率/净利率、经营性现金流、forward PE），均**未触发**重试、
  **无** `no_quantitative_anchor`。**601985 从 0617 的「无锚 0」脱 0 为「有锚 3.0」**，且可逐步追溯到
  驱动判断（「营收 CAGR 6.97% < 10% 门槛、净利率趋势下降、PE > 20」）。
- **#65 成立：新增「东财盈利预测」可达源真实供料 + 覆盖度元数据可见 + 不可达源静默跳过不崩。**
  两 A 股标的均命中 forecast（19 研报/13 买入·6 增持、2025–2028 逐年预测每股收益），素材条数与
  consensus 质量抬升——601985 sentiment 55→65、Gap −5.5%→−2.0%；600150 narrative 直接引用
  「机构一致看好，盈利预测逐年增长」。公告(cninfo)等不可达源**静默跳过并计入未覆盖**。
- **没有再引入「无关财务冒充验证」**：越界口径（AI 算力/装机/分部/重置成本/回购）一律 qualitative/
  due_diligence；n_quant 在 `_enforce_proxy_capability` 之后统计，与 S3 共用唯一真源
  `is_proxy_computable()`。

## 二、#8 前后对比（our_growth 是否脱 0、是否有锚）

| 标的 | 0617 our_growth | 0617 锚状态 | 本轮 our_growth | 本轮 n_quant | 本轮 quant 驱动 condition | 本轮锚状态 |
|---|---|---|---|---|---|---|
| 601985.SH 中国核电 | 0.0 | **无锚**（S2 全外部/分部口径，无引擎可验证驱动） | **3.0** | 2 | 营收CAGR≥15%·净利润CAGR≥15%；毛利率≥40%·OCF 为正 | **有锚** |
| 600150.SH 中国船舶 | 0.0 | 无锚（0617：错配假支持修成诚实尽调后→0） | 0.05 | 2 | 营收CAGR≥10%·净利率逐升；forward PE<20 | **有锚** |
| 0700.HK 腾讯控股 | 0.0 | 0617 仅 PE 1 环 quant，our_growth 仍 0 | 0.0 | 2 | 净利润同比增长率持续为正；forward PE≥20 | **有锚** |

- **机制**：S2 prompt 强制「至少 1 条公司级财务驱动项」并复用 S3 白名单措辞 + 正/反例 + 反循环论证
  （不得把命题结论/kill-criterion 当带权环节）。三标的首轮即产出 ≥2 条驱动 → 未触发重试。
- **诚实性**：our_growth=3.0/0.05/0.0 均**有锚、可追溯**到驱动判断；非凭空数字。601985 的 3.0 来自
  「营收 CAGR 仅 6.97% 低于门槛、净利率下降」——驱动被真实检验后给出的克制估计（不是编造非 0）。
- **退路已测（离线）**：「全外部口径」thesis 触发一次有界重试（enforce_driver=True）；重试仍无驱动 →
  打 `no_quantitative_anchor` 并让 S5 显式说明「无定量锚」，**绝不伪造 quantitative**（见
  `tests/test_thesis_pipeline.py::TestS2DriverRetry`）。本轮真实标的均未走到该退路（S2 首轮即给驱动）。

## 三、#65 前后对比（A 股素材覆盖度变化）

| 标的 | 覆盖源（命中>0） | 未覆盖（跳过/失败/空） | 最终素材条数 | forecast 命中内容 |
|---|---|---|---|---|
| 601985.SH | research, news, **forecast** | irm, rating, announcement(跳过), cls | 4（0617≈3） | 研报数 19；买入 13/增持 6；2025–2028 预测 EPS 0.452/0.431/0.489/0.546 |
| 600150.SH | news, **forecast** | research, irm, rating, announcement(跳过), cls | 3 | 机构一致看好、盈利预测逐年增长（直接进 key_narrative） |

- **新增可达源**：`ProfitForecastFetcher`（东财 `stock_profit_forecast_em` 全市场一致预期表 → 按代码/
  名称过滤到本标的，整轮缓存复用）。走 datacenter.eastmoney（本网络可达），实测 ~17s 取 1 条逐股
  一致预期（含近六月评级分布 + 多年预测 EPS）——直接喂 sentiment/implied_growth。
  > 纠偏：`stock_profit_forecast_em(symbol=...)` 的 symbol 是**行业板块名**而非股票代码，首版误传代码
  > 导致 akshare 内部 `'NoneType' object is not subscriptable`（已优雅降级为空、未崩）；改用全市场表
  > +过滤后实测真实供料（见 commit `fix(text): profit-forecast uses market table + target filter`）。
- **可达性分类 + 降级**：research/news/rating/forecast/沪市 e互动=可达；公告(cninfo)/深市互动易=不可达
  →`skip_unreachable_sources`（默认 True）静默跳过、计入未覆盖；财联社=uncertain（有 30s 超时护栏，
  仍尝试）。三标的真实运行**coordinator 降级 0 崩溃**。
- **覆盖度元数据可见（§五 #9）**：每标的产出 `TextCoverage`（逐源命中条数 + covered/uncovered +
  过薄阈值降级标注），`get_last_coverage()` 取用、协调器 INFO/WARNING 落日志、main 过薄时 WARNING。
  > P2 可达机器把 `skip_unreachable_sources: false` 即尝试全部源（含公告/深市互动易）。
  > sina/cninfo/push2his 的网络封锁是环境问题，按交接留给 P2 专用机器，本轮**不在本机绕过**。

## 四、成本与机械链路（SDK 层实测，三标的合计 ≈ $0.0024）

| 标的 | 信号 | Gap | our/implied | Sentiment | 墙钟 | LLM 调用 | 成本(v4-flash) |
|---|---|---|---|---|---|---|---|
| 601985.SH | WAIT | −2.0% | 3.0 / 5.0 | 65 | 229.9s | 9 | $0.000841 |
| 600150.SH | WAIT | −24.9% | 0.05 / 24.95 | 70 | 277.1s | 9 | $0.000843 |
| 0700.HK | WAIT | −8.5% | 0.0 / 8.5 | 65 | 19.4s | 7 | $0.000722 |

- 16+ 次结构化 LLM 调用 **0 次 JSON 重试、0 次流水线回退、0 次 S2 驱动重试**；CSV 保持 14 列；
  单标的 $0.0007–0.0008。A 股墙钟大头仍是 akshare 财报逐页 + forecast 全表（~17s，整轮缓存复用）。

## 五、本轮改动（8 commit，每个独立 + pytest）

1. `feat(S2)`：LOGIC_CHAIN_SYSTEM 强制 ≥1 公司级财务驱动 + 白名单 + 正/反例 + 反循环论证；
   `enforce_driver` 重试后缀 + S5 `no_quantitative_anchor` 约束（prompt-only）。
2. `feat(models)`：ThesisProjection 加向后兼容 `no_quantitative_anchor` + `no_anchor_reason`。
3. `feat(llm)`：client/fake 透传 `enforce_driver` / `no_quantitative_anchor`。
4. `feat(pipeline)`：S3 后统计 n_quant；==0 一次有界 S2 重试 + 重跑 S3/enforce；仍 0 → 诚实标无锚、
   告知 S5、main 摘要呈现；唯一真源 `is_proxy_computable`。
5. `feat(text)`：可达性分类 + `TextCoverage`/`TextSourceCoverage` 元数据 + 不可达静默跳过
   （`skip_unreachable_sources`，默认 True）+ `get_last_coverage()`。
6. `feat(text)`：新增可达盈利预测源 `ProfitForecastFetcher` + 接入协调器/config。
7. `feat(main)`：A 股覆盖度过薄时运行内 WARNING 显式降级标注。
8. `fix(text)`：盈利预测改全市场表+过滤（symbol 实为行业板块名）；实测真实供料。

## 六、判据核对

- **#8**：三标的（含 0700.HK）真实重跑，「有可验证驱动」的 thesis 给出**有锚、可逐步追溯**的
  our_growth（601985 脱 0 为 3.0；600150 0.05；0700 0.0），无驱动退路诚实标 `no_quantitative_anchor`
  （离线已测）。✅
- **#65**：A 股素材条数与覆盖度元数据可见（forecast 真实供料、`TextCoverage` 落日志/产物），
  coordinator 降级不崩（不可达源静默跳过）。✅
- 全量离线 `pytest -m "not integration"` **441 passed, 5 deselected**。✅

## 七、残留（非本轮范围）

- forecast 全市场表无超时护栏（datacenter.eastmoney 实测稳定快；若日后挂起可加 cls 式 daemon-timeout）。
- 「0 LLM calls / 0 tokens」内部遥测仍为假（§五 #10，真值见 SDK 层 metrics）；sina/cninfo/push2his
  网络封锁留 P2 专用机器；RiskEngine / SQLite 默认化 / 龙虾 harness 仍属 P2，未做。
